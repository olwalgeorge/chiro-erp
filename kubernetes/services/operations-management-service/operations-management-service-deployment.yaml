apiVersion: apps/v1
kind: Deployment
metadata:
  name: operations-management-service
  namespace: chiro-erp
  labels:
    app: operations-management-service
    version: v1
    tier: backend
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: operations-management-service
  template:
    metadata:
      labels:
        app: operations-management-service
        version: v1
    spec:
      containers:
      - name: operations-management-service
        image: chiro-erp/operations-management-service:latest
        ports:
        - containerPort: 8081
          name: http
        env:
        - name: QUARKUS_HTTP_PORT
          value: "8082"
        - name: QUARKUS_PROFILE
          value: "prod"
        - name: MODULES
          value: "project, fleet, pos, fieldservice"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /q/health/live
            port: 8082
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /q/health/ready
            port: 8082
          initialDelaySeconds: 5
          periodSeconds: 5
        startupProbe:
          httpGet:
            path: /q/health/started
            port: 8082
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 30
---

