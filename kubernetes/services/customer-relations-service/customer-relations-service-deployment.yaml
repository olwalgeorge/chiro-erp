apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-relations-service
  namespace: chiro-erp
  labels:
    app: customer-relations-service
    version: v1
    tier: backend
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: customer-relations-service
  template:
    metadata:
      labels:
        app: customer-relations-service
        version: v1
    spec:
      containers:
      - name: customer-relations-service
        image: chiro-erp/customer-relations-service:latest
        ports:
        - containerPort: 8082
          name: http
        env:
        - name: QUARKUS_HTTP_PORT
          value: "8083"
        - name: QUARKUS_PROFILE
          value: "prod"
        - name: MODULES
          value: "crm, repair"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /q/health/live
            port: 8083
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /q/health/ready
            port: 8083
          initialDelaySeconds: 5
          periodSeconds: 5
        startupProbe:
          httpGet:
            path: /q/health/started
            port: 8083
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 30
---

