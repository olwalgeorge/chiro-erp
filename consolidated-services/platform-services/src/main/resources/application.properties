# Application Configuration for platform-services
quarkus.application.name=platform-services
quarkus.http.port=8084

# Logging
quarkus.log.level=INFO

# Health checks
quarkus.smallrye-health.enabled=true

# Profile-specific configurations
%dev.quarkus.log.level=DEBUG
%test.quarkus.log.level=WARN

# Database, Kafka, and Monitoring configurations (commented out until needed)
# quarkus.datasource.db-kind=postgresql
# quarkus.datasource.username=chiro_user
# quarkus.datasource.password=chiro_password
# quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/chiro_platform
# kafka.bootstrap.servers=localhost:9092
