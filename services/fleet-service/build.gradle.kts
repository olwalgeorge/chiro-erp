plugins {
    id("service-conventions")
    id("quality-conventions")
    id("testing-conventions")
}

description = "Fleet Service - Fleet and vehicle management"

dependencies {
    // Service-specific dependencies (if any)
    // Standard dependencies are inherited from service-conventions
}
