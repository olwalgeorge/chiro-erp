plugins {
    id("service-conventions")
}

// Procurement service specific dependencies (if any)
dependencies {
    // Add procurement-service specific dependencies here if needed
    // All common dependencies are inherited from service-conventions
}
