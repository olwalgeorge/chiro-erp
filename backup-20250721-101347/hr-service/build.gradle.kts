plugins {
    id("service-conventions")
}

// HR service specific dependencies (if any)
dependencies {
    // Add hr-service specific dependencies here if needed
    // All common dependencies are inherited from service-conventions
}
