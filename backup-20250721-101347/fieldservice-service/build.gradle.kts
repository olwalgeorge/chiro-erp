plugins {
    id("service-conventions")
}

// Field service specific dependencies (if any)
dependencies {
    // Add fieldservice-service specific dependencies here if needed
    // All common dependencies are inherited from service-conventions
}
