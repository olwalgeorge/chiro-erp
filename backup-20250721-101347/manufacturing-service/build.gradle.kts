plugins {
    id("service-conventions")
}

// Manufacturing service specific dependencies (if any)
dependencies {
    // Add manufacturing-service specific dependencies here if needed
    // All common dependencies are inherited from service-conventions
}
