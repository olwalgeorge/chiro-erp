plugins {
    id("service-conventions")
}

// Notifications service specific dependencies (if any)
dependencies {
    // Add notifications-service specific dependencies here if needed
    // All common dependencies are inherited from service-conventions
}
