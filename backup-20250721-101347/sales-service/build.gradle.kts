plugins {
    id("service-conventions")
}

// Sales service specific dependencies (if any)
dependencies {
    // Add sales-service specific dependencies here if needed
    // All common dependencies are inherited from service-conventions
}
