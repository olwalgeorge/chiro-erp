plugins {
    id("service-conventions")
}

// Inventory service specific dependencies (if any)
dependencies {
    // Add inventory-service specific dependencies here if needed
    // All common dependencies are inherited from service-conventions
}
