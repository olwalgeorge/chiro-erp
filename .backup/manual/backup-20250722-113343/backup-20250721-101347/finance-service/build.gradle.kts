plugins {
    id("service-conventions")
}

// Finance service specific dependencies (if any)
dependencies {
    // Add finance-service specific dependencies here if needed
    // All common dependencies are inherited from service-conventions
}
