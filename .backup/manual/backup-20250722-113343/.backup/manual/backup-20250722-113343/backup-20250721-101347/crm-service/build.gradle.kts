plugins {
    id("service-conventions")
}

// CRM service specific dependencies (if any)
dependencies {
    // Add crm-service specific dependencies here if needed
    // All common dependencies are inherited from service-conventions
}
