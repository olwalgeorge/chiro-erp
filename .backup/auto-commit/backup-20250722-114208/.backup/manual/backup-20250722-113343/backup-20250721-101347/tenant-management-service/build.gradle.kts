plugins {
    id("service-conventions")
}

// Tenant management service specific dependencies (if any)
dependencies {
    // Add tenant-management-service specific dependencies here if needed
    // All common dependencies are inherited from service-conventions
}
