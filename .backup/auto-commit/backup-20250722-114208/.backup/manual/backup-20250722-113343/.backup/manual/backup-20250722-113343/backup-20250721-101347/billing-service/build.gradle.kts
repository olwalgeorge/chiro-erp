plugins {
    id("service-conventions")
}

// Billing service specific dependencies (if any)
dependencies {
    // Add billing-service specific dependencies here if needed
    // All common dependencies are inherited from service-conventions
}
