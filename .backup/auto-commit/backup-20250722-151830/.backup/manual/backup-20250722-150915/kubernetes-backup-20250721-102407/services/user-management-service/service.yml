apiVersion: v1
kind: Service
metadata:
  name: user-management-service
  namespace: chiro-erp
spec:
  selector:
    app: user-management-service
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP
