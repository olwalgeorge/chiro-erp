apiVersion: v1
kind: Service
metadata:
  name: tenant-management-service
  namespace: chiro-erp
spec:
  selector:
    app: tenant-management-service
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP
