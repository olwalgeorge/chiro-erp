plugins {
    id("service-conventions")
}

// Project service specific dependencies (if any)
dependencies {
    // Add project-service specific dependencies here if needed
    // All common dependencies are inherited from service-conventions
}
