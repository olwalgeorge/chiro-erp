plugins {
    id("service-conventions")
}

// User management service specific dependencies (if any)
dependencies {
    // Add user-management-service specific dependencies here if needed
    // All common dependencies are inherited from service-conventions
}
