plugins {
    id("service-conventions")
}

// Analytics service specific dependencies (if any)
dependencies {
    // Add analytics-service specific dependencies here if needed
    // All common dependencies are inherited from service-conventions
}
