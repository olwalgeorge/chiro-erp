plugins {
    id("service-conventions")
}

// POS service specific dependencies (if any)
dependencies {
    // Add pos-service specific dependencies here if needed
    // All common dependencies are inherited from service-conventions
}
